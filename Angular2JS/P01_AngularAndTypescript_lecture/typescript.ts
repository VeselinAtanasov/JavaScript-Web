

let isTrue: boolean = false;
let num: number = 11;
let myname: string = 'Veselin';

/**
 * Could be any type of data
 */
let anyname: any = 'Veselin';
let arr: number[] = [1, 23];

let myage: number | string = 'Vesko';

/**
 * 
 * @param name could be string or number
 */
function solve22(name: number | string) {

}

/**
 * What is the returnment of the function:
 */

function myFunc(name: number | string): string {
    return 'Veselin'
}